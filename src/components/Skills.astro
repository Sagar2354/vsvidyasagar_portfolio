---
import Section from "./Section.astro";
import type { SkillGroupProps } from "@types";

interface Props {
  skills: SkillGroupProps[];
}

const { skills } = Astro.props;
---

<Section text="Technical Skills" href="skills">
  <p class="mb-10 max-w-3xl text-lg text-white/85 glass-text">
    Proficient in modern data engineering and analytics tooling for ingestion,
    transformation, orchestration, and insight delivery.
  </p>

  <div class="grid gap-6 md:grid-cols-2 xl:grid-cols-3">
    {
      skills.map(({ title, items }) => (
        <article class="tilt-card fade-in-section rounded-[28px] md:rounded-[36px] border border-white/10 border-t-white/20 border-l-white/20 bg-white/5 md:bg-white/10 backdrop-blur-[12px] md:backdrop-blur-[20px] p-8 shadow-[0_8px_32px_rgba(0,0,0,0.3)]">
          <h3 class="mb-4 font-serif text-2xl font-semibold text-white">{title}</h3>
          <ul class="ml-5 list-outside list-disc space-y-3">
            {items.map((item) => (
              <li class="text-base text-white/85 glass-text">{item}</li>
            ))}
          </ul>
        </article>
      ))
    }
  </div>
</Section>
